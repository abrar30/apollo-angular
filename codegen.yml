# To generate GQL Schema
overwrite: true
schema: "http://localhost:5020/hrx"
documents: "src/graphql/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
